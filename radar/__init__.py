"""Kansai Radar — autonomous, free daily event bot for the Kansai region.

A self-contained pipeline that collects public events, scores them against a
local keyword-based interest profile, sends Telegram digests and reminders, and
regenerates a public Leaflet map — all on free infrastructure (GitHub Actions),
with no paid APIs and no external database.
"""

__version__ = "1.0.0"
